# ========================================
# LinkedIn Birthday Auto Bot - Dashboard
# Environment Variables Configuration
# ========================================
#
# ⚠️  IMPORTANT: This file MUST be configured before starting the dashboard!
# ⚠️  Copy this file to .env and replace all placeholder values
#
# Quick setup:
#   1. cp .env.example .env
#   2. Generate secrets: openssl rand -hex 32
#   3. Edit .env with your values
#   4. Restart the dashboard
# ========================================

# ----------------------------------------
# 🔐 Dashboard Authentication (REQUIRED)
# ----------------------------------------
# These variables are REQUIRED for the dashboard to work.
# Without them, you will see "Erreur de connexion au serveur" on login.

# JWT Secret for session tokens
# REQUIRED - MUST be at least 32 characters
# Generate with: openssl rand -hex 32
JWT_SECRET=your_jwt_secret_minimum_32_characters_here

# Dashboard Login Credentials
# REQUIRED - Set your own secure credentials
# These are the username/password you'll use to login to the dashboard
DASHBOARD_USER=your_username
DASHBOARD_PASSWORD=your_secure_password

# ----------------------------------------
# 🔌 Backend API Configuration (REQUIRED)
# ----------------------------------------

# URL of the Python FastAPI backend
# Default: http://api:8000 (Docker internal network)
# For local development: http://localhost:8000
BOT_API_URL=http://api:8000

# API Key for authenticating requests to the backend
# REQUIRED - MUST be set to a secure random string
# Generate with: openssl rand -hex 32
BOT_API_KEY=your_secure_random_key_here_min_32_chars

# ----------------------------------------
# Security Best Practices
# ----------------------------------------

# 1. NEVER commit your actual .env file to git
# 2. Generate secure random keys:
#    - Linux/Mac: openssl rand -hex 32
#    - Node.js: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# 3. Use different keys for development and production
# 4. Rotate keys regularly in production
# 5. Use a password manager to store credentials

# ----------------------------------------
# Quick Setup Commands
# ----------------------------------------

# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Generate secure keys:
#    echo "BOT_API_KEY=$(openssl rand -hex 32)" >> .env
#    echo "JWT_SECRET=$(openssl rand -hex 32)" >> .env
#
# 3. Set your credentials:
#    Edit .env and replace DASHBOARD_USER and DASHBOARD_PASSWORD
#
# 4. Verify configuration:
#    cat .env
